import React from "react";
import { useNavigate } from "react-router-dom";

//Use this as collection of methods with redirecting functions
//Or a place to check if redirecting is allowed (whether or not user is authenticated)
export default function Navigation() {

}
